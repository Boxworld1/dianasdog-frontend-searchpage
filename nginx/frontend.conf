server {
    listen 80 default_server;
    listen [::]:80 default_server;
    root /opt/app/dist;

    location / {
    }

    location /search {
        proxy_pass http://localhost:8080/search;
    }
}
